{
  "name": "assignment-2-testing",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "moment": "^2.29.4"
  },
  "devDependencies": {
    "vitest": "^4.0.0",
    "@vitest/coverage-istanbul": "^4.0.0"
  }
}
